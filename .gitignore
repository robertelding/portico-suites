# Secrets — never commit
.env
*.pem

# Dependencies & runtime
node_modules/
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Build artefacts & archives (source lives in folders, not zips)
*.zip
